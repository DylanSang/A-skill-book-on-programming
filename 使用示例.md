# 使用示例

## 说明

这份文档提供一组可以直接复制使用的示例，帮助你把 `.agents` / `.claude` 放进真实工程目录后，在 `Codex`、`Claude Code`、`Cursor` 中快速调用这套 skill/workflow。

如果你还没看过完整说明，先阅读：

- [使用说明.md](E:/AI/skill%20V3/使用说明.md)
- [优化报告.md](E:/AI/skill%20V3/优化报告.md)

## 示例 1：Codex 中做嵌入式规划任务

### 适用场景

- 新功能规划
- 输出 `方案.md`
- 输出 `Todo.md`
- 输出 `debug.json`

### 可直接复制的提示词

```text
Use $embedded-feature-planner to analyze this embedded request.
First follow the repository CLAUDE.md, then read .claude/embedded-feature-planner/AGENTS.md.
Keep model routing disabled unless I explicitly enable it.
Generate task-packet.json first, then produce or update 方案.md、Todo.md、debug.json, and run validation.

任务：规划一个新的 OTA 容错功能，要求支持断点续传、失败回滚和升级前健康检查，并输出方案和 Todo。
```

### 预期执行路径

1. 先遵循项目根目录 `CLAUDE.md`
2. 再读 `.claude/embedded-feature-planner/AGENTS.md`
3. 生成 `task-packet.json`
4. 输出 `方案.md`、`Todo.md`、`debug.json`
5. 运行校验

## 示例 2：Claude Code 中做嵌入式调试任务

### 适用场景

- 编译失败
- 驱动不生效
- 服务异常

### 可直接复制的提示词

```text
请先遵循项目根目录 CLAUDE.md。
然后阅读 .claude/embedded-feature-planner/AGENTS.md。
按其中流程执行。
默认关闭模型路由，除非我明确要求启用。
先生成 task-packet.json，再继续。

任务：排查 DTS 属性不生效和 compatible 匹配失败的问题，给出根因、修复路径、回归方案，并同步更新 debug.json。
```

### 预期执行路径

1. 识别当前任务为 `debug`
2. 路由到 `dts_or_driver_not_effective`
3. 生成 `task-packet.json`
4. 选择最少必要角色：
   - 硬件
   - BSP
   - 驱动
   - 测试
5. 检查 DTS、binding、parse、运行时证据
6. 输出修复结论并更新 `debug.json`

## 示例 3：Cursor 中做 Prompt 生成任务

### 适用场景

- 生成专家 Prompt
- 维护 `describe_prompt.md`
- 做角色设计

### 可直接复制的提示词

```text
先遵循项目根目录 CLAUDE.md。
再阅读 .claude/expert-prompt-generator/AGENTS.md。
按该流程生成 prompt，并追加到 describe_prompt.md。
默认关闭模型路由。

任务：帮我生成一个用于“嵌入式驱动调试”的高质量 Prompt，要求适合分析 DTS、probe、Kconfig、Makefile 和 dmesg 日志。
```

### 预期输出形态

- 四行 Prompt
- 追加写入 `describe_prompt.md`
- 保留原始需求
- 标记模式为“嵌入式”

## 示例 4：显式启用模型路由

### 适用场景

- 你明确想让不同功能使用不同模型
- 你已经准备好或愿意继续完善模型路由配置

### 可直接复制的提示词

```text
先遵循项目根目录 CLAUDE.md，并阅读 .claude/embedded-feature-planner/AGENTS.md。
这次显式启用模型路由。
请使用 .claude/embedded_ai_roles/assets/model-routing-template.json 作为配置来源。
先解析 model_routing，再继续执行。

任务：排查服务崩溃和 watchdog 重启问题，输出 task-packet.json，并给出后续调试方案。
```

### 对应命令示例

```bash
python .claude/embedded_ai_roles/scripts/generate_task_packet.py --task "排查服务崩溃和 watchdog 重启问题，输出 task-packet.json，并给出后续调试方案。" --enable-model-routing --routing-config ".claude/embedded_ai_roles/assets/model-routing-template.json" --output task-packet.json
```

### 预期行为

- `model_routing.enabled = true`
- 返回功能到模型的映射
- 若 `model_id` 仍是占位符，会给出 warning

## 示例 5：最稳妥的通用模板

如果你不想区分工具，可以直接把下面这一段发给任意一个：

```text
先遵循项目根目录 CLAUDE.md。
再根据任务类型阅读：
- .claude/embedded-feature-planner/AGENTS.md
或
- .claude/expert-prompt-generator/AGENTS.md

按对应工作流执行。
默认保持模型路由关闭，除非我明确要求启用。
如果是嵌入式任务，先生成 task-packet.json，再继续正式产物和校验。
如果是 Prompt 任务，追加到 describe_prompt.md，并运行校验。

当前任务：
[把你的真实任务写在这里]
```

## 示例 6：生成 task-packet 的命令行示例

### 默认关闭模型路由

```bash
python .claude/embedded_ai_roles/scripts/generate_task_packet.py --task "规划一个新的 OTA 容错功能并输出方案和 Todo" --output task-packet.json
```

### 校验 task-packet

```bash
python .claude/embedded_ai_roles/scripts/validate_task_packet.py --packet task-packet.json
```

### 查看任务路由

```bash
python .claude/embedded_ai_roles/scripts/route_task.py --task "规划一个新的 OTA 容错功能并输出方案和 Todo" --json
```

## 示例 7：一个简化的 task-packet 结果示意

下面是一个简化后的结构示意，帮助理解输出长什么样：

```json
{
  "mode": "planning",
  "scenario": "new_feature_planning",
  "model_routing": {
    "enabled": false,
    "source": "default_off"
  },
  "playbook_section": "## 1. 新功能规划",
  "roles": [
    { "id": "product_manager" },
    { "id": "system_architect" },
    { "id": "test_engineer" },
    { "id": "project_manager" }
  ]
}
```

## 推荐先试哪个示例

如果你第一次实际使用，建议从下面两个开始：

1. 示例 1：Codex 嵌入式规划任务
2. 示例 3：Cursor Prompt 生成任务

这两个最容易快速看到效果，而且不会涉及模型路由额外复杂度。
