---
description: 交叉编译工作流 — 编译、定位、修复、验证和记录编译问题的闭环规则
globs: "**/CMakeLists.txt,**/Makefile,**/*.cmake"
alwaysApply: false
---

# 编译工作流

## 编译前先确认

- 当前工程是否是交叉编译项目
- 实际 C / C++ 编译器路径和版本
- toolchain 文件、Makefile 或脚本中的真实编译入口
- 当前产物路径和部署路径

## 编译迭代流程

1. 执行当前工程的真实编译命令
2. 读取完整错误输出
3. 判断问题属于代码、构建脚本、工具链、依赖还是环境
4. 优先修复最小必要问题
5. 重新编译并记录结果
6. 连续失败达到上限时，停止盲目重试并生成问题分析

## 故障处理要求

- 必须保留完整报错信息
- 必须说明根因判断
- 不要只改报错行而忽略源文件是否入构建、链接库是否齐全、脚本是否正确
- 若连续 5 次仍失败，生成 `编译故障分析.md` 或写入 `debug.json`

## 成功后产物记录

编译成功后至少记录：

- 编译时间
- 实际编译器和版本
- 产物路径
- 关键 warning
- 修复轮次
- 剩余风险

## 测试入口记录

编译成功后，给出最小部署与验证步骤：

- 如何运行
- 如何检查是否启动成功
- 如果失败看哪个日志或返回值

