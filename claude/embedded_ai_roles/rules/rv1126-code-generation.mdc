---
description: 嵌入式代码生成规范 — 生成或修改 C/C++ 代码、头文件和构建文件时必须遵守的模块化、兼容性与验证规则
globs: "**/*.cpp,**/*.c,**/*.h,**/CMakeLists.txt"
alwaysApply: false
---

# 代码生成规范

## 核心原则

- 优先基于当前仓库结构做最小闭环修改
- 只有当现有实现明显不可维护时，才推动重构
- 语言标准、编译器能力、模块边界以当前工程事实为准，不凭记忆写死

## 架构要求

- 优先使用 POSIX 或当前项目既有标准接口
- 新增模块应尽量保持低耦合、可单独编译、可单独测试
- 是否拆成独立进程、静态模块或插件，由当前架构和仓库事实决定
- 如果推荐 MQTT、CLI、插件化或守护进程模式，先确认当前工程是否已采用

## 文件与模块化

- 单个源文件尽量控制在可维护范围
- 超长文件优先按职责拆分，而不是继续堆积
- 公共接口放到头文件或现有公共声明位置
- 不要为了追求模板一致而硬改项目目录结构

## 配置与实现

- 用户可调参数优先与实现逻辑分离
- 新增配置项时，说明默认值、可选范围、影响面和验证方式
- 不把建议配置写成系统现有能力

## 自检与验证

- 生成代码后，至少给出一个可执行的最小验证入口
- 若项目已有自测 / smoke test / unit test 入口，优先复用
- 若没有，提供最小手工验证步骤

## 文档要求

- 接口、配置、测试入口和影响面必须能在现有文档体系中追溯
- 如果项目没有模块级 `README.md` 习惯，不强制新增
- 若当前流程要求 `方案.md`、`Todo.md`、`debug.json`，优先维护这些主产物

