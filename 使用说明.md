# 使用说明

## 目的

这份文档用于说明如何在 `Codex`、`Claude Code`、`Cursor` 中使用当前仓库已经整理好的 skill/workflow 体系。

当前体系的核心目标不是依赖某个工具“自动懂”目录结构，而是通过统一入口，让不同工具都能走同一条稳定流程。

## 先理解什么

无论在哪个工具中使用，建议都先阅读：

- [优化报告.md](E:/AI/skill%20V3/优化报告.md)

然后再按任务类型阅读对应 canonical 文件：

- 嵌入式规划 / 修复 / 调试：
  - [embedded-feature-planner/AGENTS.md](E:/AI/skill%20V3/.claude/embedded-feature-planner/AGENTS.md)
- Prompt 生成 / 角色设计 / 提示词优化：
  - [expert-prompt-generator/AGENTS.md](E:/AI/skill%20V3/.claude/expert-prompt-generator/AGENTS.md)

## 通用使用原则

所有工具统一遵守以下规则：

1. 先读 `优化报告.md`
2. 再读对应 `AGENTS.md`
3. 嵌入式任务优先先生成 `task-packet.json`
4. 模型路由默认关闭
5. 除非你明确要求，否则不要启用“不同功能使用不同模型”
6. 最后跑对应校验脚本

## 最通用的启动提示词

### 嵌入式规划 / 修复任务

可直接复制给任意工具：

```text
先阅读 优化报告.md，然后阅读 .claude/embedded-feature-planner/AGENTS.md。
按 embedded-feature-planner 工作流执行。
默认保持模型路由关闭，除非我明确要求启用。
先生成 task-packet.json，再继续输出 方案.md、Todo.md、debug.json，并运行校验。
```

### Prompt 生成任务

可直接复制给任意工具：

```text
先阅读 优化报告.md，然后阅读 .claude/expert-prompt-generator/AGENTS.md。
按 expert-prompt-generator 工作流执行。
默认保持模型路由关闭，除非我明确要求启用。
将结果追加到 describe_prompt.md，并运行校验。
```

## 在 Codex 中如何使用

### 方式 1：直接调用 skill

如果当前环境支持 repo-local skill，可直接使用：

```text
Use $embedded-feature-planner to analyze this task.
First read 优化报告.md and .claude/embedded-feature-planner/AGENTS.md.
Keep model routing disabled unless I explicitly enable it.
Generate task-packet.json first, then continue.
```

或：

```text
Use $expert-prompt-generator to generate a prompt for this request.
First read 优化报告.md and .claude/expert-prompt-generator/AGENTS.md.
Keep model routing disabled unless I explicitly enable it.
```

### 方式 2：不依赖 skill 触发

如果环境不自动识别 `$embedded-feature-planner` / `$expert-prompt-generator`，就直接显式告诉它读哪些文件：

```text
先阅读 优化报告.md 和 .claude/embedded-feature-planner/AGENTS.md。
按其中流程执行。
默认关闭模型路由。
先生成 task-packet.json，再继续。
```

### Codex 推荐场景

- 代码修改
- 调试闭环
- 任务包生成
- 角色路由与多角色整合
- Prompt 生成

## 在 Claude Code 中如何使用

### 推荐方式

`Claude Code` 最稳的方式是显式指定入口文件，不要依赖它自动猜测 `.claude` 目录语义。

嵌入式任务推荐这样发：

```text
请先阅读 优化报告.md。
然后阅读 .claude/embedded-feature-planner/AGENTS.md。
按其中流程执行。
默认关闭模型路由，除非我明确要求启用。
先生成 task-packet.json，再继续。
```

Prompt 任务推荐这样发：

```text
请先阅读 优化报告.md。
然后阅读 .claude/expert-prompt-generator/AGENTS.md。
按其中流程执行，把结果追加到 describe_prompt.md。
默认关闭模型路由。
```

### Claude Code 使用建议

- 适合显式工作流驱动
- 适合文档和实现并行推进
- 适合让它严格遵循 `AGENTS.md`

### 后续可选增强

如果后面你想进一步优化 `Claude Code` 体验，可以再新增一份 repo 根目录 `CLAUDE.md`，把：

- 先读 `优化报告.md`
- 再读对应 `AGENTS.md`
- 默认关闭模型路由

这些规则固化进去。

## 在 Cursor 中如何使用

### 推荐方式

在 `Cursor` 中最稳的是直接在 Agent/Chat 中发启动模板，而不是依赖它猜测项目约定。

嵌入式任务：

```text
先阅读 优化报告.md。
再阅读 .claude/embedded-feature-planner/AGENTS.md。
按该流程处理当前任务。
默认关闭模型路由。
先生成 task-packet.json，再执行后续步骤。
```

Prompt 任务：

```text
先阅读 优化报告.md。
再阅读 .claude/expert-prompt-generator/AGENTS.md。
按该流程生成 prompt，并追加到 describe_prompt.md。
默认关闭模型路由。
```

### Cursor 使用建议

- 把上面的启动提示词保存成常用模板
- 每次新会话先贴一遍
- 不要默认让它自由发挥项目规范

## 模型路由如何使用

### 默认状态

当前模型路由默认关闭。

也就是说，如果你没有明确说“启用模型路由”或“不同功能使用不同模型”，系统应保持：

- `model_routing.enabled = false`
- `source = default_off`

模型路由策略见：

- [model-routing-policy.md](E:/AI/skill%20V3/.claude/embedded_ai_roles/references/model-routing-policy.md)

### 何时启用

只有在以下情况才建议启用：

- 你明确想把 `planning`、`debug`、`artifact_review`、`prompt_generation` 分给不同模型
- 你已经准备好了可用的模型配置
- 你愿意接受多模型带来的额外复杂度

### 启用模型路由的表达方式

对任意工具都可以这样说：

```text
这次显式启用模型路由。
请使用 .claude/embedded_ai_roles/assets/model-routing-template.json 作为配置来源。
先解析 model_routing，再继续执行。
```

### 对应命令

嵌入式任务：

```bash
python .claude/embedded_ai_roles/scripts/generate_task_packet.py --task "<任务描述>" --enable-model-routing --routing-config ".claude/embedded_ai_roles/assets/model-routing-template.json" --output task-packet.json
```

Prompt 任务：

```bash
python .claude/embedded_ai_roles/scripts/resolve_model_routing.py --skill expert-prompt-generator --task "<用户需求>" --enable-model-routing --routing-config ".claude/embedded_ai_roles/assets/model-routing-template.json" --json
```

### 当前限制

当前模板里的 `model_id` 仍是占位符，尚未绑定你的真实模型体系。

因此现在的模型路由能力是：

- 结构已完成
- 开关已完成
- 校验已完成
- 真实模型选择仍需你后续补全

## 推荐标准流程

### 嵌入式任务标准流程

1. 让工具读 `优化报告.md`
2. 让工具读 `.claude/embedded-feature-planner/AGENTS.md`
3. 明确“默认关闭模型路由”
4. 先生成 `task-packet.json`
5. 再生成或更新 `方案.md`、`Todo.md`、`debug.json`
6. 跑校验脚本

### Prompt 任务标准流程

1. 让工具读 `优化报告.md`
2. 让工具读 `.claude/expert-prompt-generator/AGENTS.md`
3. 明确“默认关闭模型路由”
4. 生成四行 prompt
5. 追加到 `describe_prompt.md`
6. 跑校验脚本

## 常用命令

### 看任务路由

```bash
python .claude/embedded_ai_roles/scripts/route_task.py --task "<任务描述>" --json
```

### 看最少必要角色

```bash
python .claude/embedded_ai_roles/scripts/select_roles.py --task "<任务描述>"
```

### 生成任务包

```bash
python .claude/embedded_ai_roles/scripts/generate_task_packet.py --task "<任务描述>" --output task-packet.json
```

### 校验任务包

```bash
python .claude/embedded_ai_roles/scripts/validate_task_packet.py --packet task-packet.json
```

### 校验模型路由配置

```bash
python .claude/embedded_ai_roles/scripts/validate_model_routing_config.py --config ".claude/embedded_ai_roles/assets/model-routing-template.json"
```

### 校验 planner 产物

```bash
python .claude/embedded-feature-planner/scripts/validate_planning_artifacts.py --workspace .
```

### 校验 prompt 产物

```bash
python .claude/expert-prompt-generator/scripts/validate_prompt_output.py --describe-file describe_prompt.md
```

## 当前最推荐的实践

如果你希望三种工具都用起来尽量一致，推荐固定执行以下约定：

1. 每次会话先贴“启动提示词”
2. 始终显式告诉它先读 `优化报告.md`
3. 始终显式告诉它读对应 `AGENTS.md`
4. 始终显式说明模型路由是否启用
5. 嵌入式任务先 `task-packet.json`
6. 最后显式要求跑校验

## 一句话建议

把这套体系当成“统一工作流入口”，不要把它当成“某个特定 IDE 的专属插件”。

只要每次都先读：

- `优化报告.md`
- 对应 `AGENTS.md`

再明确：

- 模型路由默认关闭
- 先生成任务包或追加 prompt
- 最后做校验

那么在 `Codex`、`Claude Code`、`Cursor` 中都可以稳定复用。
