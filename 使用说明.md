# 使用说明

## 目的

这份文档说明如何把当前仓库中的 `.agents` / `.claude` 内容直接放到一个真实工程目录下，并在 `Claude Code`、`Codex`、`Cursor` 中作为项目内工作流直接使用。

重点不是“研究这个 skill 仓库本身”，而是“把这套工作流搬进你的项目后，怎样直接开工”。

## 适用方式

这套内容推荐按“项目内直接落地”的方式使用。

也就是把下面这些目录或文件复制到你的目标工程根目录：

- `.claude/`
- `.agents/`

推荐同时带上：

- `CLAUDE.md`

可选带上：

- `优化报告.md`
- `skill-iterations-test/`

## 最小可运行拷贝

如果你的目标只是“在工程目录里直接让工具按 workflow 工作”，最少建议保留：

- `.claude/`
- `.agents/`
- `CLAUDE.md`

作用分别是：

- `.claude/`
  - canonical 工作流、references、rules、scripts、模板
- `.agents/`
  - skill wrapper 与 UI metadata
- `CLAUDE.md`
  - 仓库级入口规则，帮助 `Claude Code` / `Codex` / `Cursor` 在进入项目后优先走正确主流程

如果你还希望保留“总览导航”和“回归验证样例”，再加上：

- `优化报告.md`
- `skill-iterations-test/`

## 放到工程目录后，谁是真正入口

把内容放到你的工程目录后，真正的执行入口仍然是 canonical 文件，而不是 wrapper 文案本身。

优先级如下：

1. `CLAUDE.md`
2. `.claude/embedded-feature-planner/AGENTS.md`
3. `.claude/expert-prompt-generator/AGENTS.md`
4. `.claude/.../references/*`
5. `.claude/.../scripts/*`
6. `.agents/skills/*/SKILL.md`

也就是说：

- `.claude/.../AGENTS.md` 是执行真源
- `.agents/.../SKILL.md` 主要负责触发和导航
- `.agents/.../agents/openai.yaml` 主要负责 UI 展示和默认 prompt

## 两种典型落地方式

### 方式 1：只把 workflow 带进业务工程

适合你已经有自己的代码仓库，只想把这套 AI 工作流直接放进去。

目标工程目录建议长这样：

```text
your-project/
├── .claude/
├── .agents/
├── CLAUDE.md
├── src/ 或已有工程文件
└── 其他业务目录
```

这种方式下，AI 工具进入工程目录后，就可以直接：

- 读取 `CLAUDE.md`
- 读取 `.claude/.../AGENTS.md`
- 对当前工程做分析、规划、修复、输出产物

### 方式 2：把验证与总览也一起带进工程

适合你希望这套内容在目标工程里不仅能工作，还能长期维护和回归验证。

目标工程目录建议长这样：

```text
your-project/
├── .claude/
├── .agents/
├── CLAUDE.md
├── 优化报告.md
├── skill-iterations-test/
├── src/ 或已有工程文件
└── 其他业务目录
```

这种方式除了直接使用，还可以：

- 跑校验脚本
- 看 `current-state-index.md`
- 做 wrapper 漂移检查
- 做 fixture 回归

## 在 Claude Code 中如何直接使用

如果目标工程根目录里已经有：

- `.claude/`
- `.agents/`
- `CLAUDE.md`

那么进入工程目录后，推荐直接这样使用。

### 嵌入式规划 / 修复 / 调试

```text
请先遵循项目根目录的 CLAUDE.md。
然后阅读 .claude/embedded-feature-planner/AGENTS.md。
按 embedded-feature-planner 工作流处理当前工程任务。
默认关闭模型路由，除非我明确要求启用。
如果是复杂嵌入式任务，先生成 task-packet.json，再继续输出正式产物。
```

### Prompt 生成 / 角色设计

```text
请先遵循项目根目录的 CLAUDE.md。
然后阅读 .claude/expert-prompt-generator/AGENTS.md。
按 expert-prompt-generator 工作流处理当前任务。
默认关闭模型路由，结果追加到 describe_prompt.md。
```

### Claude Code 使用要点

- 不要依赖它自动猜 `.claude` 的含义，最好显式要求先读 `CLAUDE.md`
- 对嵌入式任务，优先要求先生成 `task-packet.json`
- 对 prompt 任务，优先要求追加到 `describe_prompt.md`

## 在 Codex 中如何直接使用

如果目标工程支持 repo-local skill，可直接调用 `.agents` 中的 wrapper。

### 方式 1：显式使用 skill 名

嵌入式任务：

```text
Use $embedded-feature-planner for this project task.
First follow the repository CLAUDE.md and then read .claude/embedded-feature-planner/AGENTS.md.
Keep model routing disabled unless I explicitly enable it.
Generate task-packet.json first, then continue with the project artifacts.
```

Prompt 任务：

```text
Use $expert-prompt-generator for this request.
First follow the repository CLAUDE.md and then read .claude/expert-prompt-generator/AGENTS.md.
Keep model routing disabled unless I explicitly enable it.
Append the result to describe_prompt.md.
```

### 方式 2：不依赖 skill 自动触发

如果当前环境不自动识别 `$embedded-feature-planner` / `$expert-prompt-generator`，直接这样说即可：

```text
先遵循项目根目录 CLAUDE.md。
再阅读 .claude/embedded-feature-planner/AGENTS.md。
按其中工作流执行。
默认关闭模型路由。
先生成 task-packet.json，再继续。
```

## 在 Cursor 中如何直接使用

`Cursor` 最稳的方式仍然是把启动模板直接贴给 Agent / Chat。

### 嵌入式任务

```text
先遵循项目根目录 CLAUDE.md。
再阅读 .claude/embedded-feature-planner/AGENTS.md。
按该工作流处理当前工程任务。
默认关闭模型路由。
先生成 task-packet.json，再执行后续步骤。
```

### Prompt 任务

```text
先遵循项目根目录 CLAUDE.md。
再阅读 .claude/expert-prompt-generator/AGENTS.md。
按该工作流生成 prompt，并追加到 describe_prompt.md。
默认关闭模型路由。
```

### Cursor 使用要点

- 把上面的启动模板保存成常用模板
- 每次新会话优先贴“先遵循 CLAUDE.md”
- 不要默认让它自由发挥项目规范

## 这套内容在工程目录里实际会产出什么

如果你把它放在真实工程里使用，常见产物包括：

- `task-packet.json`
- `方案.md`
- `Todo.md`
- `debug.json`
- `describe_prompt.md`
- `role-summary.md`

这些文件默认应写在工程根目录，而不是 `.claude` 或 `.agents` 目录中。

## 嵌入式任务的推荐直接用法

在任意工具里，都可以先用这段最小启动提示词：

```text
先遵循项目根目录 CLAUDE.md。
然后阅读 .claude/embedded-feature-planner/AGENTS.md。
按 embedded-feature-planner 工作流执行。
默认关闭模型路由，除非我明确要求启用。
先生成 task-packet.json，再继续输出 方案.md、Todo.md、debug.json，并运行校验。

任务：
[把真实嵌入式任务写在这里]
```

## Prompt 任务的推荐直接用法

```text
先遵循项目根目录 CLAUDE.md。
然后阅读 .claude/expert-prompt-generator/AGENTS.md。
按 expert-prompt-generator 工作流执行。
默认关闭模型路由，除非我明确要求启用。
将结果追加到 describe_prompt.md，并运行校验。

任务：
[把真实 prompt 需求写在这里]
```

## 模型路由在工程目录中怎么处理

默认规则不变：

- 模型路由默认关闭
- 只有你明确要求“启用模型路由”或“不同功能使用不同模型”时才启用

常见表达方式：

```text
这次显式启用模型路由。
请使用 .claude/embedded_ai_roles/assets/model-routing-template.json 作为配置来源。
先解析 model_routing，再继续执行。
```

如果目标工程里还没有真实模型映射，那么它当前依然只是：

- 结构已完成
- 默认关闭已完成
- 校验已完成
- 真实 `model_id` 仍需你自己补齐

## 建议一起带过去的校验命令

如果目标工程里保留了这些脚本，推荐常用下面几条：

```bash
python .claude/embedded_ai_roles/scripts/validate_skill_wrappers.py
python .claude/embedded_ai_roles/scripts/validate_task_packet.py --task "规划一个新的 OTA 容错功能并输出方案和 Todo"
python .claude/embedded_ai_roles/scripts/validate_model_routing_config.py --config ".claude/embedded_ai_roles/assets/model-routing-template.json"
python .claude/embedded_ai_roles/scripts/generate_current_state_index.py --check skill-iterations-test/current-state-index.md
```

如果目标工程里没有带 `skill-iterations-test/`，那就保留前 3 条，去掉最后一条。

## 是否必须带上 优化报告.md

不是必须，但推荐。

不带也可以直接工作，因为真正的主流程仍然在：

- `CLAUDE.md`
- `.claude/.../AGENTS.md`

带上的收益是：

- 新接手的人更容易快速理解这套结构
- 更容易知道哪些能力稳定、哪些能力仍是占位
- 更容易理解为什么默认关闭模型路由

## 一句话落地建议

如果你要把这套内容放进一个真实项目，最推荐的做法是：

1. 把 `.claude/`、`.agents/`、`CLAUDE.md` 放进工程根目录
2. 进入工程后先让工具遵循 `CLAUDE.md`
3. 再按任务类型读取对应 `.claude/.../AGENTS.md`
4. 嵌入式任务先生成 `task-packet.json`
5. Prompt 任务追加到 `describe_prompt.md`
6. 默认关闭模型路由
7. 最后跑校验

这样这套 workflow 就能在 `Claude Code`、`Codex`、`Cursor` 中作为“工程内直接可用能力”稳定复用，而不是只在这个 skill 仓库里可用。
