# 优化报告

## 目的

这份报告用于把当前 workspace 中多轮 skill / role / workflow 优化结果压缩成一份可快速上手的总览，避免后续继续迭代时需要反复翻阅长对话或多份阶段性报告。

适用对象：

- 后续继续优化这套 skill 的人
- 需要快速理解当前 `.claude` / `.agents` 结构的人
- 需要知道“当前已经做到哪里、下一步最值得做什么”的人

## 当前结论

当前这套体系已经从“静态提示词/角色模板”迭代成一套更接近工作流引擎的结构，具备以下能力：

1. 有 canonical 主流程
2. 有按需加载的 references
3. 有最少必要角色选择
4. 有任务级 playbook
5. 有任务包生成与校验
6. 有默认关闭、显式启用的模型路由
7. 有 fixture 和脚本化验证链

当前最重要的入口不再是对话，而是以下 canonical 文件：

- [embedded-feature-planner/AGENTS.md](E:/AI/skill%20V3/.claude/embedded-feature-planner/AGENTS.md)
- [expert-prompt-generator/AGENTS.md](E:/AI/skill%20V3/.claude/expert-prompt-generator/AGENTS.md)

## 目录现状

当前工作目录核心结构只有三层：

- `.claude`
  - canonical 工作流、角色、规则、参考、脚本、模板
- `.agents`
  - skill wrapper 与 UI metadata
- `skill-iterations-test`
  - fixture、校验样例、阶段性报告

## 已完成优化概览

### 1. 两个核心 skill 已完成 canonical 化

已稳定的两个 skill：

- `embedded-feature-planner`
- `expert-prompt-generator`

它们的真实执行逻辑都以 `.claude/.../AGENTS.md` 为准，`.agents/skills/*/SKILL.md` 主要承担轻量入口和 wrapper 说明。

### 2. planner 已从提示词进化成任务工作流

`embedded-feature-planner` 当前具备：

- success criteria 前置
- evidence-first 门禁
- planning / implementation / debug 三模式
- 任务路由
- 最少必要角色集合
- `task-packet.json` 中间契约
- `方案.md` / `Todo.md` / `debug.json` 产物闭环
- 任务完成后的结构化校验

### 3. 多角色体系已从“模板集合”进化成“按需协作系统”

`.claude/embedded_ai_roles` 当前具备：

- 10 个角色文件
- 共享规则参考
- 角色输出契约
- 角色选择与路由
- 角色输出汇总
- playbook
- 文档校验器

设计方向已经从“默认全量加载角色”改成：

1. 先走共享规则
2. 再选最少必要角色
3. 必要时才扩角色

### 4. 模型路由已接入，但默认关闭

当前模型路由已经实现，但遵循严格边界：

- 默认 `disabled`
- 只有用户明确要求时才启用
- 启用后必须结构化记录
- 错误配置不会偷偷生效

模型路由策略见：

- [model-routing-policy.md](E:/AI/skill%20V3/.claude/embedded_ai_roles/references/model-routing-policy.md)

## 当前关键文件地图

### 主流程入口

- [embedded-feature-planner/AGENTS.md](E:/AI/skill%20V3/.claude/embedded-feature-planner/AGENTS.md)
- [expert-prompt-generator/AGENTS.md](E:/AI/skill%20V3/.claude/expert-prompt-generator/AGENTS.md)

### planner 关键参考

- [official-design-principles.md](E:/AI/skill%20V3/.claude/embedded-feature-planner/references/official-design-principles.md)
- [evidence-and-safety.md](E:/AI/skill%20V3/.claude/embedded-feature-planner/references/evidence-and-safety.md)
- [workflow-and-outputs.md](E:/AI/skill%20V3/.claude/embedded-feature-planner/references/workflow-and-outputs.md)

### roles 关键参考

- [shared-foundation.md](E:/AI/skill%20V3/.claude/embedded_ai_roles/references/shared-foundation.md)
- [role-output-contracts.md](E:/AI/skill%20V3/.claude/embedded_ai_roles/references/role-output-contracts.md)
- [role-routing-and-selection.md](E:/AI/skill%20V3/.claude/embedded_ai_roles/references/role-routing-and-selection.md)
- [role-review-checklist.md](E:/AI/skill%20V3/.claude/embedded_ai_roles/references/role-review-checklist.md)
- [role-examples.md](E:/AI/skill%20V3/.claude/embedded_ai_roles/references/role-examples.md)
- [task-playbooks.md](E:/AI/skill%20V3/.claude/embedded_ai_roles/references/task-playbooks.md)
- [model-routing-policy.md](E:/AI/skill%20V3/.claude/embedded_ai_roles/references/model-routing-policy.md)

### 核心脚本

- [select_roles.py](E:/AI/skill%20V3/.claude/embedded_ai_roles/scripts/select_roles.py)
- [route_task.py](E:/AI/skill%20V3/.claude/embedded_ai_roles/scripts/route_task.py)
- [task_routing_core.py](E:/AI/skill%20V3/.claude/embedded_ai_roles/scripts/task_routing_core.py)
- [generate_task_packet.py](E:/AI/skill%20V3/.claude/embedded_ai_roles/scripts/generate_task_packet.py)
- [validate_task_packet.py](E:/AI/skill%20V3/.claude/embedded_ai_roles/scripts/validate_task_packet.py)
- [resolve_model_routing.py](E:/AI/skill%20V3/.claude/embedded_ai_roles/scripts/resolve_model_routing.py)
- [validate_model_routing_config.py](E:/AI/skill%20V3/.claude/embedded_ai_roles/scripts/validate_model_routing_config.py)
- [assemble_role_output.py](E:/AI/skill%20V3/.claude/embedded_ai_roles/scripts/assemble_role_output.py)
- [validate_role_docs.py](E:/AI/skill%20V3/.claude/embedded_ai_roles/scripts/validate_role_docs.py)
- [validate_rule_docs.py](E:/AI/skill%20V3/.claude/embedded_ai_roles/scripts/validate_rule_docs.py)
- [validate_reference_links.py](E:/AI/skill%20V3/.claude/embedded_ai_roles/scripts/validate_reference_links.py)
- [validate_playbooks.py](E:/AI/skill%20V3/.claude/embedded_ai_roles/scripts/validate_playbooks.py)
- [validate_planning_artifacts.py](E:/AI/skill%20V3/.claude/embedded-feature-planner/scripts/validate_planning_artifacts.py)
- [validate_prompt_output.py](E:/AI/skill%20V3/.claude/expert-prompt-generator/scripts/validate_prompt_output.py)

### 配置与模板

- [model-routing-template.json](E:/AI/skill%20V3/.claude/embedded_ai_roles/assets/model-routing-template.json)

### 已有阶段性报告

- [skill-iteration-report.md](E:/AI/skill%20V3/skill-iterations-test/skill-iteration-report.md)
- [claude-roles-iteration-report.md](E:/AI/skill%20V3/skill-iterations-test/claude-roles-iteration-report.md)

## 当前能力状态

### 已稳定

- canonical 主流程
- references 拆分与按需加载
- 角色输出结构
- 最少必要角色路由
- 任务级 playbook
- task packet 生成与校验
- prompt 生成四行契约
- 模型路由默认关闭机制
- 状态索引自动汇总最近一次产物摘要

### 已验证

- wrapper 与 canonical 入口一致性校验
- 角色文档校验
- 规则文档校验
- references 断链校验
- playbook 结构校验
- task routing fixture
- task packet fixture
- model routing fixture
- planning artifact fixture
- prompt fixture
- Windows 中文 JSON 子进程编码稳定性

### 仍是模板 / 占位状态

- 模型路由模板中的 `model_id` 仍是占位符
- 当前还没有与你实际使用模型体系绑定
- 当前仓库不是 Git repo，因此 Git 相关流程只有规则，没有在真实仓库里完整演练
- 状态索引已经能摘要最近一次产物，但当 workspace 根目录没有真实产物时，仍会回退到 fixture 快照

## 快速使用方式

### 1. 看当前任务应该怎么走

```bash
python .claude/embedded_ai_roles/scripts/route_task.py --task "<任务描述>" --json
```

### 2. 看最少必要角色集合

```bash
python .claude/embedded_ai_roles/scripts/select_roles.py --task "<任务描述>"
```

### 3. 生成开工任务包

```bash
python .claude/embedded_ai_roles/scripts/generate_task_packet.py --task "<任务描述>" --output task-packet.json
```

### 4. 显式启用模型路由

只有明确要求时才这样做：

```bash
python .claude/embedded_ai_roles/scripts/generate_task_packet.py --task "<任务描述>" --enable-model-routing --routing-config ".claude/embedded_ai_roles/assets/model-routing-template.json" --output task-packet.json
```

### 5. 校验任务包

```bash
python .claude/embedded_ai_roles/scripts/validate_task_packet.py --packet task-packet.json
```

### 6. 校验模型路由配置

```bash
python .claude/embedded_ai_roles/scripts/validate_model_routing_config.py --config ".claude/embedded_ai_roles/assets/model-routing-template.json"
```

### 7. planner 产物校验

```bash
python .claude/embedded-feature-planner/scripts/validate_planning_artifacts.py --workspace .
```

### 8. prompt 产物校验

```bash
python .claude/expert-prompt-generator/scripts/validate_prompt_output.py --describe-file describe_prompt.md
```

## 当前设计原则摘要

这套体系目前遵循的关键原则是：

1. success criteria 前置
2. 证据优先，不凭记忆补全
3. 主文件短，细节放 references
4. 角色按需加载，不全量加载
5. 输出契约必须显式
6. 复杂任务先路由，再执行，再校验
7. 高成本能力默认关闭，需显式启用

## 当前最重要风险

1. 模型路由还没有绑定真实模型名
2. 如果后续继续大改 `.claude/.../AGENTS.md`，仍需要跑 wrapper 校验器确认 `.agents/skills/*` 没有漂移
3. 当前没有真实 Git 工程仓库场景来完整验证 `.git` 相关工作流
4. 状态索引已经覆盖最近一次产物摘要，但真实项目历史仍未沉淀成可连续追踪的执行记录

## 建议下一步

下一轮最值得做的方向建议优先级如下。

### 优先级 1

把模型路由模板里的占位 `model_id` 替换成你真实要用的模型体系，并明确：

- 哪个模型负责 `planning`
- 哪个模型负责 `debug`
- 哪个模型负责 `artifact_review`
- 哪个模型负责 `prompt_generation`

### 优先级 2

继续增强“状态索引自动生成器”，自动汇总：

- 当前 skill 能力
- 当前脚本清单
- 当前 fixture 状态
- 最近一次验证结果
- 最近一次真实任务产物摘要
- 多次真实任务的连续历史

这样后续就不仅不需要手工维护总览，也能从“最近一次快照”继续演进到“连续历史索引”。

### 优先级 3

把模型路由继续细化成：

- skill 级默认配置
- 场景级覆盖
- 角色级推荐模型

但仍然保持“默认关闭”。

### 优先级 4

在真实 Git 工程目录中做一次完整演练，验证：

- `.git` 检查
- `task-packet.json`
- `方案.md` / `Todo.md` / `debug.json`
- 角色汇总
- 模型路由关闭态 / 开启态

## 与现有报告的关系

这份 `优化报告.md` 不是替代现有阶段性报告，而是它们的压缩导航版：

- 需要看阶段细节：去看 `skill-iterations-test/*.md`
- 需要快速知道现在能做什么：先看这份 `优化报告.md`

## 本次新增价值

这次新增这份报告后，后续继续优化时可以先看这一份，就能快速回答以下问题：

- 当前真正的入口文件是什么
- 哪些能力已经落地
- 哪些能力仍是占位
- 默认关闭的模型路由现在怎么工作
- 下一轮最值得继续打磨哪里
